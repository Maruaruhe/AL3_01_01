#include "Bullet.h"
#include "assert.h"
void Initialize(Model* model, const Vector3& position) { 
	assert(model);
	model_ = model;
}

void Update() {

}

void Draw(const ViewProjection& viewProjection) {

}